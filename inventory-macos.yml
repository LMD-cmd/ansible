all:
  children:
    macos_hosts:
      hosts:
        mac-mini-m1:
          ansible_host: 10.0.200.24
          ansible_user: luiscruzeiro
          ansible_become_password: "{{ vault_macos_sudo_password | default(omit) }}"

        macbook-pro-m2:  # ← Neuer Hostname (frei wählbar)
          ansible_host: 10.0.200.159  # ← IP-Adresse des neuen MacBooks
          ansible_user: luiscruzeiro  # ← Username auf dem MacBook
          ansible_become_password: "{{ vault_macos_sudo_password | default(omit) }}"